# Use with ANDROID-SKILL: Practical Prompt Examples

## Supabase example

Prompt:

```text
Use with ANDROID-SKILL: Fix UnauthorizedRestException in my Android edge function flow.
```

Expected skill focus:
- `supabase-android`
- JWT handling, `persistSession`, `getUser(jwt)`, error mapping

## Compose example

Prompt:

```text
Use with ANDROID-SKILL: Build a Compose screen with LazyColumn, bottom sheet, and proper insets.
```

Expected skill focus:
- `compose-ui`
- edge-to-edge, scaffold inner padding, nav bar padding, one-shot events

## Hilt example

Prompt:

```text
Use with ANDROID-SKILL: Set up Hilt injection for a Repository and @HiltViewModel.
```

Expected skill focus:
- `hilt-di`
- module binding, scopes, ViewModel injection, testing patterns

## Architecture example

Prompt:

```text
Use with ANDROID-SKILL: Refactor this feature to MVVM + UDF with UiState and UiEvent.
```

Expected skill focus:
- `android-architecture`
- layer boundaries, single UiState flow, event channel, repository contracts
